version: v1
kind: Spicepod
name: http_tvmaze

datasets:
  - from: https://api.tvmaze.com
    name: tvmaze
    params:
      file_format: json
      client_timeout: 30s
      allowed_request_paths: '/shows/**,/search/people'
      request_query_filters: enabled
