version: v1beta1
kind: Spicepod
name: tpch-write
datasets:
  - from: s3://spiceai-demo-datasets/tpch/customer/
    name: customer
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from customer limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/lineitem/
    name: lineitem
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from lineitem limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/nation/
    name: nation
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from nation limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/orders/
    name: orders
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from orders limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/part/
    name: part
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from part limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/partsupp/
    name: partsupp
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from partsupp limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/region/
    name: region
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from region limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}

  - from: s3://spiceai-demo-datasets/tpch/supplier/
    name: supplier
    params:
      file_format: parquet
    acceleration:
      enabled: true
      engine: postgresql
      refresh_sql: select * from supplier limit 1000
      params:
        pg_host: ${secrets:PG_HOST}
        pg_port: 5439
        pg_sslmode: prefer
        pg_db: dev
        pg_user: ${secrets:PG_USER}
        pg_pass: ${secrets:PG_PASS}
